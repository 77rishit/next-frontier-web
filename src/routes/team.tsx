import { createFileRoute } from "@tanstack/react-router"; import { PageHero,SiteShell,Frame,SourceLink } from "../components/SiteShell"; import { contacts,coordinators } from "../data/site"; import teamImage from "../assets/team-command.jpg";
export const Route=createFileRoute("/team")({head:()=>({meta:[{title:"Team — Techfest IIT Bombay"}]}),component:Page}); function Page(){return <SiteShell><PageHero code="07 / OPERATIONS" title="People behind" accent="the future." copy="The current team roster published by Techfest IIT Bombay." image={teamImage}/><section className="team-section"><h2>Overall coordinators</h2><div className="team-roster">{coordinators.map(([n,r],i)=><Frame key={n} className="person-card"><div className={`portrait portrait-${i}`}/><small>TF // OC-0{i+1}</small><h3>{n}</h3><p>{r}</p></Frame>)}</div><h2>Department managers</h2><div className="team-roster managers">{contacts.map(([r,n,e,p],i)=><Frame key={e} className="person-card"><div className={`portrait portrait-${i%4}`}/><small>TF // M-{String(i+1).padStart(2,"0")}</small><h3>{n}</h3><p>{r}</p><a href={`mailto:${e}`}>{e}</a><a href={`tel:${p.replace(/\s/g,"")}`}>{p}</a></Frame>)}</div><SourceLink href="https://techfest.org/contactus"/></section></SiteShell>}
