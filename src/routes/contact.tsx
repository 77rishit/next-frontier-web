import { createFileRoute } from "@tanstack/react-router"; import { Mail,Phone } from "lucide-react"; import { PageHero,SiteShell,Frame,SourceLink } from "../components/SiteShell"; import { contacts } from "../data/site";
export const Route=createFileRoute("/contact")({head:()=>({meta:[{title:"Contact — Techfest IIT Bombay"}]}),component:Page}); function Page(){return <SiteShell><PageHero code="09 / COMMS" title="Open a" accent="channel." copy="Reach the relevant current Techfest department manager directly." image="/src/assets/powai-world.jpg"/><section className="contact-section"><div className="notice"><b>Routing note</b><p>The official website publishes department contacts rather than one general enquiry address. Choose the closest team below.</p></div><div className="contact-grid">{contacts.map(([r,n,e,p],i)=><Frame key={e}><small>CHANNEL // {String(i+1).padStart(2,"0")}</small><h2>{r}</h2><h3>{n}</h3><a href={`mailto:${e}`}><Mail/>{e}</a><a href={`tel:${p.replace(/\s/g,"")}`}><Phone/>{p}</a></Frame>)}</div><SourceLink href="https://techfest.org/contactus"/></section></SiteShell>}
